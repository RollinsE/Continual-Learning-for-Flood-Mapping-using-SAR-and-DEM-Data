# Streamlit Community Cloud uses this file because it sits beside the app entrypoint.
# Keep this deployment environment intentionally smaller than the full training stack.
-e .
streamlit==1.60.0
torch>=2.2,<3.0
torchvision>=0.17,<1.0
timm>=1.0,<2.0
numpy>=1.26,<2.4
opencv-python-headless>=4.8
rasterio>=1.3,<1.6
matplotlib>=3.5
PyYAML>=6.0
pydantic>=1.10,<3.0
